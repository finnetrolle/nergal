global:
  # SMTP configuration for email alerts (optional)
  # smtp_smarthost: 'smtp.example.com:587'
  # smtp_from: 'alerts@nergal-bot.example.com'
  # smtp_auth_username: 'alerts@nergal-bot.example.com'
  # smtp_auth_password: 'your-password'

  # Slack configuration (optional)
  # slack_api_url: 'https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK'

# Templates
templates:
  - '/etc/alertmanager/templates/*.tmpl'

# Inhibition rules - prevent certain alerts when another is firing
inhibit_rules:
  - source_matchers:
      - severity="critical"
    target_matchers:
      - severity="warning"
    equal: ['alertname', 'instance']

# Route tree
route:
  # Default receiver
  receiver: 'default-receiver'
  
  # Group alerts by these labels
  group_by: ['alertname', 'severity']
  
  # Wait time before sending initial notification
  group_wait: 30s
  
  # Wait time before sending updated notification
  group_interval: 5m
  
  # Wait time before resending notification
  repeat_interval: 4h

  # Child routes
  routes:
    # Critical alerts - send immediately
    - matchers:
        - severity="critical"
      receiver: 'critical-alerts'
      group_wait: 10s
      repeat_interval: 1h
      continue: true

    # Warning alerts
    - matchers:
        - severity="warning"
      receiver: 'warning-alerts'
      group_wait: 1m
      repeat_interval: 4h

    # LLM-related alerts
    - matchers:
        - alertname=~"LLM.*"
      receiver: 'llm-alerts'
      group_by: ['alertname', 'provider']

# Receivers
receivers:
  - name: 'default-receiver'
    # Default receiver - configure your preferred notification method
    webhook_configs:
      - url: 'http://localhost:5001/webhook'
        send_resolved: true

  - name: 'critical-alerts'
    # Critical alerts - use multiple channels
    # Email (configure SMTP above)
    # email_configs:
    #   - to: 'admin@example.com'
    #     send_resolved: true
    
    # Slack (configure slack_api_url above)
    # slack_configs:
    #   - channel: '#alerts-critical'
    #     send_resolved: true
    #     title: '{{ .Status | toUpper }}: {{ .CommonAnnotations.summary }}'
    #     text: '{{ .CommonAnnotations.description }}'
    
    # Telegram bot for alerts (recommended)
    # Create a separate bot for alerts and use its token
    webhook_configs:
      - url: 'http://localhost:5001/webhook/critical'
        send_resolved: true

  - name: 'warning-alerts'
    # Warning alerts
    webhook_configs:
      - url: 'http://localhost:5001/webhook/warning'
        send_resolved: true

  - name: 'llm-alerts'
    # LLM-specific alerts
    webhook_configs:
      - url: 'http://localhost:5001/webhook/llm'
        send_resolved: true
